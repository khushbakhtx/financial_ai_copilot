"use client";

import React from "react";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import { MarkdownContent } from "@/app/components/MarkdownContent";

const USER_GUIDE_CONTENT = `# Руководство пользователя — Financial AI Copilot

> Этот документ предназначен для пользователей без технической подготовки. Здесь объяснено, что умеет агент, какие задачи он решает и как с ним работать.

---

## Что такое Financial AI Copilot?

Financial AI Copilot — это интеллектуальный помощник для построения финансовых моделей машинного обучения. Вы просто описываете задачу обычным языком, загружаете данные — и агент самостоятельно проводит весь процесс: от анализа данных до готовой модели.

Никаких знаний в программировании или математике не требуется.

---

## Какие задачи умеет решать агент?

### 1. Кредитный скоринг (оценка риска дефолта)
Агент строит модель, которая определяет вероятность того, что заёмщик не вернёт кредит. Используется для принятия решений по выдаче займов.

**Пример:** У вас есть данные о 20 000 клиентах с историей погашений. Агент построит модель, которая для нового клиента выдаст оценку риска от 0 до 100%.

---

### 2. Коллекторный скоринг
Агент строит модель, которая оценивает вероятность возврата долга по просроченному кредиту, или прогнозирует сумму возврата.

**Пример:** Вам нужно расставить приоритеты в работе с должниками — агент определит, с кем больше шансов договориться.

---

### 3. Обнаружение мошенничества
Агент строит модель для выявления мошеннических транзакций или заявок среди множества обычных.

**Пример:** Из миллиона транзакций нужно выявить подозрительные. Агент обучит модель, которая будет их автоматически помечать.

---

### 4. Провизии (МСФО 9 / Резервирование)
Агент помогает оценить ожидаемые кредитные потери: вероятность дефолта (PD), уровень потерь при дефолте (LGD) и объём подверженности риску (EAD).

**Пример:** Для регуляторной отчётности нужно рассчитать резервы по кредитному портфелю — агент построит модели для каждого компонента.

---

## Какие модели использует агент?

Агент автоматически выбирает подходящий алгоритм. Пользователю не нужно разбираться в этих названиях — достаточно описать задачу.

| Тип задачи | Алгоритмы |
|---|---|
| Классификация (да/нет, дефолт/не дефолт) | CatBoost, Логистическая регрессия, LSTM, Liquid NN |
| Регрессия (прогноз числового значения) | CatBoost, Случайный лес, LSTM |
| Кластеризация (сегментация клиентов) | K-means, Агломеративная кластеризация |
| Генерация синтетических данных | ZGAN |

По умолчанию агент использует **CatBoost** — он лучше всего работает с финансовыми табличными данными.

---

## Как проходит работа с агентом?

### Шаг 1 — Загрузка данных
Вы загружаете файл с данными (CSV, Excel, Parquet) или подключаете базу данных.

### Шаг 2 — Подготовка данных
Агент анализирует ваши данные, настраивает обработку и задаёт уточняющие вопросы (например, какой столбец является целевым показателем).

### Шаг 3 — Анализ данных
Агент проводит разведочный анализ: находит аномалии, пропуски, нежелательные корреляции и отчитывается перед вами.

### Шаг 4 — Обучение модели
Агент обучает модель, проверяет её на наличие утечек данных и переобучения, при необходимости повторяет процесс (до 5 итераций).

### Шаг 5 — Результат
Вы получаете готовую модель с метриками качества и отчётом. Модель можно применить к новым данным или развернуть в рабочей среде.

---

## Что агент делает автоматически (без вашего участия)?

- Определяет тип задачи (классификация, регрессия, кластеризация)
- Выбирает алгоритм под задачу
- Обнаруживает и исключает «утечки» данных (когда модель случайно видит ответ в обучающих данных)
- Делит данные на обучающую и тестовую выборки
- Оценивает качество модели
- Повторяет обучение, если первый результат неудовлетворительный
- Фиксирует решения и пишет отчёт

---

## Что агент НЕ умеет делать?

- **Не работает с неструктурированными данными** — фотографиями, голосом, текстовыми документами. Только таблицы.
- **Не строит модели без данных** — минимум несколько сотен строк с историческими наблюдениями.
- **Не предсказывает рынки и цены акций** — агент обучается на ваших исторических данных, а не на внешних рыночных данных.
- **Не принимает кредитные решения** — агент строит модель, которая выдаёт вероятность. Окончательное решение остаётся за человеком.
- **Не исправляет некачественные данные автоматически** — если в данных много ошибок или пропусков, агент предупредит, но исправить их должны вы.
- **Не обучает на данных менее ~200 строк** — слишком мало для надёжной модели.
- **Не работает с временными рядами по умолчанию** — разделение на обучающую/тестовую выборки происходит случайно, если вы явно не попросите использовать временное разделение.

---

## Форматы данных

Агент принимает файлы в форматах:
- **CSV** (\`.csv\`)
- **Excel** (\`.xlsx\`, \`.xls\`)
- **Parquet** (\`.parquet\`)
- **Подключение к базе данных** (PostgreSQL и другие) с возможностью написать SQL-запрос

### Требования к данным:
- Каждая строка — одно наблюдение (клиент, транзакция, заявка)
- Каждый столбец — один признак
- Должен быть столбец с целевым показателем (например: \`default_flag\`, \`is_fraud\`, \`recovery_amount\`)
- Допускаются пропущенные значения — агент с ними справится

---

## Режимы работы

| Режим | Описание |
|---|---|
| **Интерактивный** | Агент спрашивает подтверждение на каждом важном шаге |
| **Автономный** | Агент работает самостоятельно, прерывается только для согласования шага настройки данных |

По умолчанию используется интерактивный режим. Если вы хотите, чтобы агент работал без остановок — скажите ему об этом.

---

## Частые вопросы

**Сколько времени занимает обучение модели?**
Обычно 2–5 минут для стандартного набора данных (10 000–50 000 строк). Генерация синтетических данных (ZGAN) может занять 10–30 минут.

**Нужно ли самому выбирать алгоритм?**
Нет. Агент выбирает автоматически. Но если у вас есть предпочтения (например, логистическая регрессия для регуляторной интерпретируемости) — просто скажите об этом.

**Что значит «Gini» и «AUC»?**
Это оценки качества модели. Gini > 0.60 считается хорошим результатом для финансового скоринга. Агент всегда объяснит, что означают метрики в контексте вашей задачи.

**Можно ли использовать синтетические данные?**
Да. Если ваша выборка мала или нужно пополнить данные для редкого события (например, мошенничества) — агент может сгенерировать синтетические данные, не нарушая конфиденциальность.

**Как применить готовую модель к новым клиентам?**
Загрузите новый файл с теми же столбцами (без столбца с ответом) — агент автоматически применит модель и вернёт результаты.

---

*Руководство актуально для Financial AI Copilot v3. По вопросам обращайтесь к команде поддержки.*
`;

interface UserGuideDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function UserGuideDialog({ open, onOpenChange }: UserGuideDialogProps) {
  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="max-h-[80vh] max-w-3xl flex flex-col">
        <DialogHeader>
          <DialogTitle>Руководство пользователя</DialogTitle>
        </DialogHeader>
        <div className="flex-1 overflow-y-auto pr-1">
          <MarkdownContent content={USER_GUIDE_CONTENT} />
        </div>
      </DialogContent>
    </Dialog>
  );
}
